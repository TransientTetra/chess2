#include <controller/chess_2.hpp>

// Main code
int main(int argc, char** argv)
{
	Chess2 application(DEFAULT_WINDOW_TITLE, DEFAULT_WINDOW_HEIGHT, DEFAULT_WINDOW_WIDTH);
	application.run();
	return 0;
}
